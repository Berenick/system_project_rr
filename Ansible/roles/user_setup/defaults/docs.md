# Значение по умолчанию
 Здесь лежат переменные с нименьшим приоритетом
 Данные значения можно переопределить извне group_vars; host_vars

 # Состав:
 <!-- Имя пользователя (с значением по умолчанию, например, deployer).UID пользователя (с дефолтным значением).
 Имя группы.
 GID группы.
 Shell пользователя.
 Флаг создания домашнего каталога.
 Путь к публичному SSH-ключу (или сам ключ как переменная).
 Флаг идемпотентности для ключей (заменять все или добавлять). -->