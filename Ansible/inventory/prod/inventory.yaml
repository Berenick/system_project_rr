all:
  children:
    nginx_server:
      hosts: 
        nginx_host_01:
          ansible_host: 192.168.10.244
          ansible_user: deploy
          ansible_ssh_private_key_file: /home/vscode/.ssh/deployment
          ansible_connection: ssh
          ansible_become: true
          ansible_become_method: sudo
          ansible_become_user: root
          ansible_become_pass: "{{ sudo_password }}"
    db_server:
      hosts:
